#For computational functions on arrays
import numpy as np

#For visualization
import matplotlib.pyplot as plt

#For functions on cleaning, exploring and manipulating datasets 
import pandas as pd

#SimpleImputer from Scikit-learn for completing missing values with different strategies
from sklearn.impute import SimpleImputer

#For encoding of categorical data for independent variables
from sklearn.compose import ColumnTransformer
from sklearn.preprocessing import OneHotEncoder

#For encoding of categorical data for dependent variable or label
from sklearn.preprocessing import LabelEncoder

#For splitting dataset into Training and Test set
from sklearn.model_selection import train_test_split

#For feature scaling
from sklearn.preprocessing import StandardScaler


